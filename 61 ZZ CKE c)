#include <iostream>
#include <fstream>
#include <vector>
#include <cmath>
#include <algorithm>
using namespace std;
int main() {
    const int N = 20;
    const int M = 1000;
    const int DL = 1000;
    int tab[N][M];
    int dlugosci[DL];
    int ile;
    int nr = 0;
    ifstream plik("bledne.txt");
    ofstream wynik1("wynik1.txt");
    ofstream wynik2("wynik2.txt");
    ofstream wynik3("wynik3.txt");
    while(plik >> ile) {
        dlugosci[nr] = ile;
        for(int i = 0; i < ile; i++) {
            plik >> tab[nr][i];
        }
        nr++;
    }
    for(int i = 0; i < nr; i++) {
        int roznica = 0;
        int r1 = tab[i][1] - tab[i][0];
        int r2 = tab[i][2] - tab[i][1];
        if(r1 == r2) {
            roznica = r1;
        } else {
            int r3 = tab[i][3] - tab[i][2];
            if(r3 == r1) {
                roznica == r1;
            } else {
                roznica = r2;
            }
        }
        for(int j = 1; j < dlugosci[i]; j++) {
            if(tab[i][j] - tab[i][j-1]!= roznica) {
                wynik3 << tab[i][j] << endl;
                break;
            }
        }
    }
    
}
