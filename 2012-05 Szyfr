#include <iostream>
#include <string>
#include <fstream>
#include <algorithm>
using namespace std;
int main() {
    const int N = 120;
    ifstream plik("tj.txt");
    ifstream plik2("klucze1.txt");
    ifstream plik3("sz.txt");
    ifstream plik4("klucze2.txt");
    ofstream wyniki("wyniki4_1.txt");
    string jawny[N];
    string klucz[N];
    string niejawny[N];
    string klucz2[N];
    for(int i = 0; i < N; i++) {
        plik >> jawny[i];
    }
    for(int i = 0; i < N; i++) {
        plik2 >> klucz[i];
    }
    for(int i = 0; i < N; i++) {
        plik3 >> niejawny[i];
    }
    for(int i = 0; i < N; i++) {
        plik4 >> klucz2[i];
    }
    wyniki << "ZADANIE 1" << endl;
    for(int i = 0; i < N; i++) {
        string szyfrogram = "";
        for(int j = 0; j<jawny[i].length(); j++) {
            int ascci = jawny[i][j];
            int przesuniecie = klucz[i][j%klucz[i].length()] - 'A' + 1;
            int nowyKod = ascci + przesuniecie;
            if(nowyKod > 90) {
                nowyKod -= 26;
            }
            szyfrogram += char(nowyKod);
        }
        wyniki << szyfrogram << endl;
    }
    wyniki << "ZADANIE 2" << endl;
    for(int i = 0; i < N; i++) {
        string szyfrogram = "";
        for(int j = 0; j < niejawny[i].length(); j++) {
            int ascci = niejawny[i][j];
            int przesuniecie = klucz2[i][j%klucz2[i].length()] - 'A' + 1;
            int nowyKod = ascci - przesuniecie;
            if(nowyKod < 65) {
                nowyKod+=26;
            }
            szyfrogram += char(nowyKod);
        }
        wyniki << szyfrogram << endl;
    }
    wyniki.close();
    return 0;
}
